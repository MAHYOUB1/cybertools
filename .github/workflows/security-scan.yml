name: Security DAST Scan (OWASP ZAP)

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  zap_security_scan:
    name: Run App and Scan with ZAP
    runs-on: ubuntu-latest

    steps:
      # ترقية الأداة إلى v4 لمنع التحذيرات والتوافق مع البيئة الحديثة
      - name: Checkout Code
        uses: actions/checkout@v4

      # تعديل الأمر إلى docker compose (بمسافة وبدون شرطة)
      - name: Start Application Services
        run: |
          docker compose up -d

      - name: Wait for Services to be Healthy
        run: |
          echo "Waiting for services to boot up..."
          sleep 15

      - name: Run OWASP ZAP Baseline Scan
        run: |
          mkdir -p zap-reports
          docker run --rm \
            --network cybertools-secure-network \
            -v $(pwd)/zap-reports:/zap/wrk/:rw \
            owasp/zap2docker-stable zap-baseline.py \
            -t http://cybertools-frontend:80 \
            -r security-report.html

      # تعديل الأمر هنا أيضاً إلى docker compose (بدون شرطة)
      - name: Stop and Clean Up Containers
        if: always()
        run: |
          docker compose down -v --rmi local

      - name: Upload ZAP Scan Report
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: zap-security-report
          path: zap-reports/security-report.html