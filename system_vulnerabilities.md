# تقرير نظام CyberTools التعليمي والثغرات المدمجة به

## 1. نظرة عامة على النظام (System Overview)
نظام **CyberTools** هو منصة تجارة إلكترونية (E-commerce Platform) صُممت خصيصاً لأغراض تعليمية وتدريبية في مجال الأمن السيبراني. 
تم بناء النظام باستخدام:
- **الواجهة الأمامية (Frontend):** HTML, CSS (بتصميم Cyberpunk), و JavaScript.
- **الواجهة الخلفية (Backend):** PHP لتقديم RESTful APIs.
- **قاعدة البيانات:** MySQL.

الهدف الأساسي من المشروع هو توفير بيئة واقعية تحاكي أخطاء البرمجة الشائعة، مما يسمح للمتدربين باكتشاف واستغلال ثغرات الويب (مثل التي تصنف ضمن OWASP Top 10)، ثم التعلم على كيفية ترقيع هذه الثغرات (Patching) وكتابة كود برمجي آمن.

---

## 2. حصر الثغرات الموجودة في النظام وطرق استغلالها

يحتوي الكود المصدري في المجلد `backend/api/` على 18 نقطة ضعف (ثغرة تعليمية) رئيسية بالإضافة إلى بعض الثغرات الأخرى المدمجة عمداً. وفيما يلي حصر كامل لها وكيفية استغلالها:

### أ. ثغرات حقن قواعد البيانات (SQL Injection)
1. **في صفحة تسجيل الدخول (`auth.php` - ثغرة #1):**
   - **الوصف:** يتم دمج اسم المستخدم وكلمة المرور مباشرة في استعلام الـ SQL دون استخدام الاستعلامات المحضرة (Prepared Statements).
   - **طريقة الاستغلال:** يمكن للمهاجم تسجيل الدخول كمسؤول (Admin) دون معرفة كلمة المرور عبر إدخال `admin'--` أو `' OR '1'='1'--` في حقل اسم المستخدم.
2. **في البحث عن المنتجات (`products.php` - ثغرة #3):**
   - **الوصف:** يُستخدم معامل `search` الممرر عبر الرابط مباشرة في استعلام البحث.
   - **طريقة الاستغلال:** استخراج بيانات إضافية مخفية عن طريق تمرير استعلامات مثل `?search=' UNION SELECT * FROM users--`.
3. **في تصفية الفئات (`products.php` - ثغرة #4):**
   - **الوصف:** يتم دمج معرف الفئة `category` مباشرة في الاستعلام.
   - **طريقة الاستغلال:** تمرير `?category=1 OR 1=1` لتخطي التصفية ورؤية نتائج إضافية.

### ب. التخزين غير الآمن للبيانات الحساسة (Cryptographic Failures)
4. **تخزين كلمات المرور كنص صريح (`auth.php` - ثغرة #2):**
   - **الوصف:** عند تسجيل مستخدم جديد، تُحفظ كلمة المرور في قاعدة البيانات كنص صريح (Plain Text) دون استخدام التشفير (مثل `bcrypt`).
   - **طريقة الاستغلال:** إذا تمكن المهاجم من الوصول إلى قاعدة البيانات (عبر استغلال SQLi)، ستصبح جميع حسابات المستخدمين مكشوفة فوراً.
5. **تخزين بيانات البطاقات الائتمانية (`payments.php` - ثغرة #11):**
   - **الوصف:** يقوم النظام بحفظ أرقام البطاقات الائتمانية بشكل كامل في جدول المدفوعات.
   - **طريقة الاستغلال:** استخراج قاعدة البيانات سيؤدي إلى تسريب معلومات مالية حساسة جداً للضحايا.

### ج. التحكم بالوصول والإشارة المباشرة للكائنات (Broken Access Control & IDOR)
6. **الوصول لمنتجات غير مفعلة (`products.php` - ثغرة #5):**
   - **الوصف:** جلب منتج معين برقم الـ `id` لا يتحقق من كونه مفعلاً `is_active=1`.
   - **طريقة الاستغلال:** يمكن للمستخدم العادي تمرير رقم لأي منتج تم إخفاؤه أو تعطيله وقراءة تفاصيله.
7. **تحديث وحذف منتجات بائعين آخرين (`products.php` - ثغرات #7 و #8):**
   - **الوصف:** الـ API لا يتحقق من أن البائع الذي يقوم بالتعديل هو المالك الحقيقي للمنتج.
   - **طريقة الاستغلال:** يمكن لأي بائع تعديل/حذف منتج يخص بائع آخر بتغيير الـ `id` في طلب الـ PUT/DELETE.
8. **الوصول لطلبات مستخدمين آخرين (`orders.php` - ثغرة #9):**
   - **الوصف:** لا يتم التحقق من ملكية الطلبات (Orders).
   - **طريقة الاستغلال:** يمكن للمهاجم قراءة تفاصيل ومشتريات المستخدمين الآخرين بتغيير معرف الطلب في الرابط.
9. **التلاعب بسلة المشتريات (`cart.php`):**
   - **الوصف:** السماح بحذف/تعديل العناصر من السلة دون تأكيد أنها تتبع للمستخدم الحالي.
   - **طريقة الاستغلال:** تمرير معرف Cart Item يخص مستخدم آخر لتخريب سلة مشترياته.
10. **رؤية بيانات أي مستخدم (`users.php` - ثغرة #14):**
    - **الوصف:** مسار جلب تفاصيل الحساب يتيح لأي مستخدم جلب تفاصيل أي حساب آخر.
    - **طريقة الاستغلال:** جلب بيانات حساب الإدارة بتغيير رقم الـ ID.
11. **الوصول لإحصائيات الإدارة (`admin/stats.php` - ثغرة #17):**
    - **الوصف:** يتم التحقق فقط من حالة تسجيل الدخول `requireAuth()` بدلاً من التحقق من صلاحيات المدير `requireRole('admin')`.
    - **طريقة الاستغلال:** وصول غير مصرح للمستخدمين العاديين للمسارات الإدارية الحساسة.

### د. ثغرات حقن السكربتات (Cross-Site Scripting - XSS)
12. **XSS في المراجعات والتقييمات (`reviews.php` و `products.php` - ثغرات #6 و #12 و #13):**
    - **الوصف:** تُحفظ التعليقات وتُعرض في صفحة المنتج بدون تحويل الرموز إلى كيانات HTML عبر دوال مثل `htmlspecialchars`.
    - **طريقة الاستغلال:** يمكن للمهاجم كتابة مراجعة تحتوي على `<script>alert(document.cookie)</script>`. سيتم تنفيذ السكربت عند زيارة أي شخص لصفحة المنتج وسرقة جلساته (Stored XSS).
13. **XSS في الملف الشخصي (`users.php` - ثغرة #15):**
    - **الوصف:** تحديث بيانات الملف الشخصي كالاسم أو الوصف يتم بدون تنقية.
    - **طريقة الاستغلال:** حقن أكواد خبيثة تنعكس في صفحة العرض الشخصية.

### هـ. الرفع غير الآمن للملفات (Insecure File Upload)
14. **رفع صور الملف الشخصي (`users.php` - ثغرة #16):**
    - **الوصف:** رفع الملفات لا يتضمن أي فلاتر للتحقق من نوع الملف (MIME Type) أو امتداده.
    - **طريقة الاستغلال:** يمكن للمهاجم رفع سكربت خبيث (Web Shell) بامتداد `.php` بدلاً من صورة، ثم استدعاء مسار الملف للتحكم الكامل بالخادم (RCE).

### و. تزوير الطلبات عبر المواقع (Cross-Site Request Forgery - CSRF)
15. **غياب الـ CSRF Tokens (`payments.php` و `products.php` - ثغرات #8 و #10):**
    - **الوصف:** النظام لا يستخدم رموز حماية للنماذج أو الطلبات التي تُغير حالة البيانات.
    - **طريقة الاستغلال:** يمكن للمهاجم إنشاء صفحة ويب خبيثة مخفية تجبر الضحية الموثقة (مسجلة الدخول) على تنفيذ طلب دفع أو حذف منتجات دون علمها عند زيارة الصفحة.

### ز. حقن الأوامر (Command Injection)
16. **حقن النظام (`admin/stats.php` - ثغرة #18):**
    - **الوصف:** استخدام إدخال غير آمن داخل دوال تنفيذ نظام التشغيل مثل `shell_exec()` أو `system()` لاستعراض إحصائيات النظام.
    - **طريقة الاستغلال:** إضافة فاصلة منقوطة وإدراج أمر إضافي، مثل: `127.0.0.1; ls -la` أو إرسال أوامر اختراق عكسي.

---

## 3. الخطوات التالية (التدريب والتصحيح)
يُنصح باتباع الخطوات التالية لتحقيق أقصى استفادة من هذا النظام كبيئة تعليمية:
1. **الاكتشاف:** استخدم أدوات مثل *Burp Suite*, *OWASP ZAP*, أو *Postman* لتأكيد واكتشاف هذه الثغرات عملياً.
2. **الاستغلال (Exploitation):** جرّب إنشاء هجمات حقيقية لاختبار مدى خطورة كل ثغرة.
3. **التصحيح (Patching):** راجع ملفات الـ PHP المذكورة أعلاه، وابحث عن العلامات `[ثغرة تعليمية]`، ثم قم بتعديل الكود وفقاً للحلول الآمنة المكتوبة في التعليقات (مثل استخدام `password_hash` و Prepared Statements).
